### First java project:

**Requirements:**

```yml
Java 8
Maven
Spring:
    Spring Context
    Spring Boot Web
```

**Examples**

Quick run
```sh
mvn clean package -Dmaven.test.skip=true && java -jar target/app.jar
```